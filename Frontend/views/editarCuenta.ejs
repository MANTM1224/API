<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar tu cuenta</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <div class="container-form">
        <h2>Edita tu cuenta</h2>
        <form id="editAccountForm" action="/editarCuenta/<%= usuario.ID %>" method="POST">
            <div class="form-group">
                <label for="username">Nombre de usuario:</label>
                <input type="text" id="username" name="username" onkeypress="return verificar(event)" required value="<%= usuario.UserName %>">
            </div>
            <div class="form-group">
                <label for="email">Correo electrónico:</label>
                <input type="email" id="email" name="email" onkeypress="return verificarCorreoE(event)" required value="<%= usuario.Correo %>">
            </div>
            <div class="form-group">
                <label for="password">Contraseña:</label>
                <input type="password" id="password" name="password" onkeypress="return verificar(event)" required value="<%= usuario.Contraseña %>">
            </div>
            <div class="form-group">
                <label for="confirmPassword">Confirmar contraseña:</label>
                <input type="password" id="confirmPassword" name="confirmPassword" onkeypress="return verificar(event)" required value="<%= usuario.Contraseña %>">
            </div>
            <button type="submit" onclick="return validarEditarCuenta()">Guardar cambios</button>
        </form>
        <a href="/cuenta/<%= usuario.ID %>" class="btn-regresar">Cancelar</a>
    </div>

  
    <script src="/js/validaciones.js"></script>
</body>
</html>